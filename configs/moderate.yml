# Moderate strategy: slightly lower floors than default
# Only overrides differ from defaults in battcontrol/config.py

price_floor_anchors:
  shoulder:
    - { price_cents: 8,  soc_floor_pct: 50 }
    - { price_cents: 10, soc_floor_pct: 33 }
    - { price_cents: 20, soc_floor_pct: 20 }
    - { price_cents: 30, soc_floor_pct: 10 }

headroom_band_high: 92
